h1 = @lesson.name
p = @lesson.description
- @lesson.materials.each do |material| 
  h3 = material.title unless material.title.nil?
  =="#{material.body}"

p 
  a (href="#{@lesson.course.ask_question_link}") Ask on Slack
